/** Locale boundary supplied by the application and by the isolated unit tests. */
declare var ppp: { i18nLocale: string };
/** Presence is checked before worker-only initialization. */
declare var WorkerGlobalScope: { new (): object };
